/**
 * Central type definitions — pure domain types, no framework dependencies.
 * ad placements, campaigns, creatives, impressions, click events, revenue records, and frequency caps
 */

// Add domain-specific types here
export {}
